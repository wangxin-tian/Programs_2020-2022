```bash
#方法一 【*V*】
#在空文件夹内，右键选择Git Bash Here
git clone  https://github.com/wangxin-tian/Programs_2020-2022.git 
#把需要上传的文件夹放入到远程仓库文件夹内
cd  boke     //根据自己的远程仓库名输入
git init
git add .
git commit -m “你的提交信息”
git push
```

